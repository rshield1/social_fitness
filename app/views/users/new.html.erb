<section id="cover" class="min-vh-100">
    <div id="cover-caption">
        <div class="container">
                <div class="col-xl-5 col-lg-6 col-md-8 col-sm-10 mx-auto text-center form p-4">
                    <h1 class="display-4 py-2 text-truncate text-primary">Sign Up</h1>
                    <div class="px-2">

                        <%= simple_form_for @user,
                          wrapper_mappings: {
                            boolean:       :custom_boolean,
                            check_boxes:   :custom_collection,
                            date:          :custom_multi_select,
                            datetime:      :custom_multi_select,
                            file:          :custom_file,
                            radio_buttons: :custom_collection,
                            range:         :custom_range,
                            time:          :custom_multi_select,
                            select:        :custom_multi_select
                          } do |f| %>
                          
                        <%= f.error_notification %>
                        <div class="form-group text-primary h5">
                          <%= f.input :username %>
                        </div>
                        <div class="form-group text-primary h5">
                          <%= f.input :email %>
                        </div>
                        <div class="form-group text-primary h5">
                          <%= f.input :password %>
                        </div>
                        <div class="form-group text-primary h5">
                          <%= f.button :submit, class: "btn-primary" %>
                          <%= f.button :button, "Cancel", type: "reset", class: "btn-danger" %>
                        </div>
                        <% end %>
                      </div>
        </div>
    </div>
    </div>
</section>


<style>
 #cover {
    background: #222 url('https://source.unsplash.com/1600x900/?fitness,people') center center no-repeat;
    background-size: cover;
    height: 100%;
    text-align: center;
    display: flex;
    align-items: center;
    position: relative;
}

#cover-caption {
    width: 100%;
    position: relative;
    z-index: 1;
}

/* only used for background overlay not needed for centering */
form:before {
    content: '';
    height: 100%;
    left: 0;
    position: absolute;
    top: 0;
    width: 100%;
    background-color: rgba(0,0,0,0.3);
    z-index: -1;
    border-radius: 10px;
}
</style>